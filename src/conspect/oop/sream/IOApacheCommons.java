package conspect.oop.sream;

import org.apache.commons.io.FileUtils;
import org.apache.commons.io.IOUtils;

import java.io.File;
import java.util.List;

public class IOApacheCommons {
    public static void main(String[] args) {
        //IOUtils.copy(r, w); // копирование
        //IOUtils.copyLarge(r, w) // копирование больших файлов
        //IOUtils.write();// запишет строку в поток
        //IOUtils.closeQuietly(stream); // закрывает поток тихо
        //FileUtils.copyDirectory(d1, d2);// скопирует файлы одной директории в другую
        //FileUtils.copyFile(f1, f2);
        //FileUtils.copyFileToDirectory(f, d);
        //FileUtils.deleteDirectory(d);// удаляет все файлы в диретории
        //FileUtils.getTempDirectory();// вернет темповскую директорию
        //FileUtils.getUserDirectory();// директория пользователя
        //FileUtils.readFileToString(f, s);// файл в строку
        //List<String> list = FileUtils.readLines(f);
    }
}
